package com.songshakes.internship.skilllist.manager

interface BottomSheetDialogFragmentListener {

    fun selectSkillCatalog(skillCatalog: String)
    fun selectSkillSubCatalog(skillSubCatalog: String)
}